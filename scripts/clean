#!/bin/bash

py3clean ./app/  # removes .pyc and .pyo files, debian-based only
rm ./app/twistd.pid  # scrapyd crashes if twistd.pid already exist
rm -r ./app/.scrapy{,d}/   # avoid potential conflicts
